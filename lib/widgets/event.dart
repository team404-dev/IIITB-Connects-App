import 'package:flutter/material.dart';
import '../models/Event.dart';

class FeedItem extends StatelessWidget {
  final Event feedItem;
  FeedItem({required this.feedItem});

  /*
  * TODO - Create items for home tab
  * */

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class ClubActivity extends StatelessWidget {
  final Event clubActivity;
  ClubActivity({required this.clubActivity});

  /*
  * TODO - Create items for club activity tab
  * */

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
